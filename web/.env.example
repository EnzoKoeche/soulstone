# Frontend (Vite) — copie para web/.env e preencha.
# A anon key é pública por design; o RLS protege o banco (só leitura para anon).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=
